# The hero recording — Jay's call script (R3)

**Call (332) 249-4333** — the live Northside Barbers demo line — once the
demo workspace is seeded (I'll confirm). You are the CALLER. The AI side is
unscripted: it's the real product, and whatever it says is what we ship.
Target: **25–40 seconds** of usable tape. Do a few takes; we keep the best.

## Recording setup
- Quiet room, phone on speaker, screen-record with system+mic audio ON
  (QuickTime/Windows Game Bar both work), or a second phone recording audio.
- Also record the app: after the call, screen-capture the Calendar view
  showing the booking that just landed (this becomes §4-1's clip).

## The script (caller side only — keep it natural, don't read robotically)

1. *(AI greets — let it finish)*
2. **"Hey — do you have anything open Thursday afternoon for a skin fade?"**
3. *(AI offers a slot or asks a follow-up — answer naturally)*
4. **"Perfect. Can I get it under Marcus?"**
5. *(AI confirms — let it finish the confirmation)*
6. **"That's all — thanks!"** *(let it close, then hang up)*

Fallback ask if Thursday is somehow full: "anything Friday morning?"

## Rules
- Don't say a real last name, email, or any real customer's info.
- If a take goes sideways (mis-hear, weird answer), just call again —
  honest tape only; we never splice words the AI didn't say.
- Keep your ask simple: ONE service, ONE day. The 3-minute demo cap is
  plenty, but tight tape cuts better.

## What I need back
1. The audio file (any format — m4a/mp3/wav).
2. Which take to use (or send all; I'll pick and you confirm).
3. The calendar screen capture after the winning take.

I'll transcribe the winning take verbatim into `/audio/demo-call.json`
(cue-timed to the audio) + `/audio/demo-call.mp3`, and the hero's transcript
panel goes live — it renders only when those files exist, so nothing fake
ever shows in the meantime.
